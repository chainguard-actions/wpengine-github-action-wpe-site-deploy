name: Test e2e Deploy to WP Engine
on:
  schedule:
    - cron: '*/5 * * * *'
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions: {}

jobs:
  run_action:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@a37ce9120846195fa4ece8f58b268e6043cb2f26 # v3
      - name: Bump test plugin version number
        run: sed -i 's/0.0.1/0.0.2/' tests/data/plugins/test-plugin/test-plugin.php
      - name: GitHub Action Deploy to WP Engine
        uses: ./
        with:
          # Deploy vars
          WPE_SSHG_KEY_PRIVATE: ${{ secrets.WPE_SSHG_KEY_PRIVATE }}
          WPE_ENV: ghae2e
          # Deploy Options
          SRC_PATH: "tests/data/plugins/test-plugin"
          REMOTE_PATH: "wp-content/plugins/"
          PHP_LINT: true
          FLAGS: -r --backup --backup-dir=/tmp --itemize-changes
          SCRIPT: "tests/data/post-deploy/test-plugin.sh"
          CACHE_CLEAR: true
      - name: Fetch deploy results
        id: fetchResult
        uses: fjogeleit/http-request-action@c0b95d02a088b47c1f2f4db04fd8af8bd19eee54 # v1
        with:
          url: "https://ghae2e.wpengine.com/wp-content/plugins/test-plugin/status.json"
      - name: Validate deploy results
        env:
          RESPONSE_STATUS: ${{ fromJson(steps.fetchResult.outputs.response).status }}
        run: |
          [ "$RESPONSE_STATUS" = "success" ] || exit 1
  notify:
    runs-on: ubuntu-latest
    if: ${{ !cancelled() }}
    needs: run_action
    permissions: {}
    steps:
      - name: Notify slack on failure
        if: needs.run_action.result == 'failure' && github.ref == 'refs/heads/main'
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}
        uses: voxmedia/github-action-slack-notify-build@a16c99c8c65bf5de29482033b665fb8656ba99a0 # v1
        with:
          # Channel: status-github-action
          channel_id: C03QDL9U0TW
          status: FAILED
          color: danger
